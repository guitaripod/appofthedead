{
  "name": "aotd-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for App of the Dead sync and authentication",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "format": "prettier --write \"src/**/*.ts\"",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^20.11.5",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2",
    "wrangler": "^4.19.1"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
